// import { PAGES_DATA } from "../pages/constans";

// const pageTitle = (location, isUserPage) => {
//   const { title } = PAGES_DATA.find(({ pathname }) => {
//     return (
//       pathname === location.pathname || pathname === isUserPage?.pattern?.path
//     );
//   });

//   return title;
// };

// export default pageTitle;
