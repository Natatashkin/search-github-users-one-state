import styled from "styled-components";

const ButtonLinkContainer = styled.div`
  margin-bottom: 20px;
`;

const UserContainer = styled.div`
  height: 100%;
  overflow: auto;
`;

const AvatarContainer = styled.div`
  width: fit-content;
  margin-left: auto;
  margin-right: auto;
`;

export { UserContainer, AvatarContainer, ButtonLinkContainer };
